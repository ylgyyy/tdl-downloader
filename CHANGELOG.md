# 更新日志 (Changelog)

本文件记录 tdl-downloader 的版本更新历史。以后每次发版，按下面的规则在文件顶部新增一条记录。

## 版本号规则

格式：`v主版本.次版本.修订号`（例如 `v1.2.3`）

| 段 | 何时 +1 | 示例 |
|----|---------|------|
| 主版本 | 不兼容的重大改动（部署方式、配置结构、环境变量等变了） | v1→v2 |
| 次版本 | 新增功能 | v1.1→v1.2 |
| 修订号 | bug 修复 / 小幅优化 | v1.2.3→v1.2.4 |

## 发版流程

1. 把本次改动按「新增 / 修复 / 优化 / 变更」分类，写成一条新记录（日期 + 版本号）
2. 把本次发布说明（Release 正文）写到 `RELEASE_NOTES.md`
3. 运行 `upload.bat v1.2.3` —— 提交 + 推送 main + 打 tag + 推送 tag
4. GitHub Actions 自动构建带版本号的镜像，并创建 GitHub Release（正文读取 `RELEASE_NOTES.md`）

## 记录格式

```
## [v1.2.3] - 2026-08-18

### 新增
- xxx 功能

### 修复
- xxx 问题

### 优化
- xxx 优化
```

---

## [v1.2.3] - 2026-08-21

### 优化
- 修正 `docker-compose.yml` 中 `MAX_CONCURRENT_DL` 的注释说明（跨账号最大并发；同账号始终串行）

## [v1.2.2] - 2026-08-21

### 修复
- 并发下载时同账号任务可能同时启动，导致 tdl 数据库锁冲突（`Current database is used by another process`）

## [v1.2.1] - 2026-08-21

### 新增
- 下载进度消息实时显示队列状态（`📊 排队 X 个 · 正在下载 Y 个`）

### 优化
- 去掉消息里 HTML 模式下不生效的 markdown 星号（下载标题、验证码提示等）
- 下载完成文件列表去掉每个文件的重复大小（总大小已有「下载大小」行）

## [v1.2.0] - 2026-08-21

### 新增
- 并发下载队列：支持同时提交多个下载任务，全局并发上限（`MAX_CONCURRENT_DL`，默认 2）+ 排队
- 同一 tdl 账号强制串行，不同账号可并行
- `/cancel` 取消本人全部下载任务（排队 + 进行中）

### 变更
- 返回主菜单不再中止正在进行的下载

## [v1.1.0] - 2026-08-18

### 新增
- 直接发送 `https://t.me/...` 消息链接即可触发单文件下载（保留原文件名，跳过重命名确认）
- 下载完成消息新增 `📦 下载大小`，文件列表每个文件也带各自大小（只统计本次新下载的文件）

## [v1.0.0] - 2026-08-18

### 新增
- 单文件下载 / 批量下载
- 下载类型过滤（图片 / 视频 / 文档白名单）
- 单文件下载时自定义重命名
- 多租户账号隔离，机器人内完成登录（手机号 + 验证码 + 2FA）
- Docker 部署 + GitHub Actions 自动构建并推送镜像到 Docker Hub
